## 0.0.1

Initial macOS implementation of the `screen_recorder` federated plugin:
ScreenCaptureKit-based live recording with VideoToolbox H.264 encoding,
microphone + system-audio capture, region/source/window pickers, cursor
state stream, and the `recording` method channel + cursor/audio event
channels.
