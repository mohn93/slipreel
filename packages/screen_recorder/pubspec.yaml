name: screen_recorder
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Local packages
  screen_recorder_platform_interface:
    path: ../screen_recorder_platform_interface
  screen_recorder_macos:
    path: ../screen_recorder_macos
  slipreel_engine:
    path: ../slipreel_engine

  # Video processing
  path_provider: ^2.1.4
  # TODO: Add FFmpeg integration in Phase 4
  # Note: ffmpeg_kit_flutter has download issues, will use system ffmpeg or alternative

  # State management
  flutter_riverpod: ^2.5.1

  # Video playback
  video_player: ^2.9.2
  # Direct dep so melos generates a workspace path override to our vendored,
  # patched copy (packages/video_player_avfoundation). Version is what
  # video_player resolves transitively; the path override bypasses it.
  video_player_avfoundation: ^2.8.9

  # Persistence
  shared_preferences: ^2.2.0

  # Logging
  logger: ^2.5.0

  # Deep-link to system settings for permission prompts
  url_launcher: ^6.2.0
  file_selector: ^1.1.0

  # Path manipulation — replaces inline _basename/_fileExtension helpers
  path: ^1.9.0

  # SHA-256 verification for downloaded whisper model binary.
  crypto: ^3.0.3

  # Injectable wall-clock so AppAlertsController's auto-dismiss timers stay
  # testable; imported directly in lib (depend_on_referenced_packages).
  clock: ^1.1.1

  # App version / build-number for the About section
  package_info_plus: ^8.0.0

  # Declarative, physics-first animations. Used for the CTA spinner that
  # rotates inside the Export button while an export is running.
  cue: ^0.2.1

  lucide_icons_flutter: ^3.1.14+2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  path_provider_platform_interface: ^2.1.0
  fake_async: ^1.3.1
  # Imported directly by tests (MockPlatformInterface fakes + url_launcher
  # mocks); declare them instead of relying on transitive resolution.
  plugin_platform_interface: ^2.1.8
  url_launcher_platform_interface: ^2.3.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Shaders moved with the engine package (P0-3 phase 2). slipreel_engine
  # declares them; this app picks them up via the package dependency.

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/cursors/default_cursor.png
    - assets/cursors/click_cursor.png
    - assets/wallpapers/macos/
    - assets/wallpapers/spring/
    - assets/wallpapers/sunset/
    - assets/wallpapers/abstract/
    # Device-frame bezels (Apple Design Resources) — licensed for distribution
    # (Apple sign-off received 2026-06-23).
    - assets/device_frames/
    - assets/device_frames/iphone-16/
    - assets/device_frames/iphone-16-plus/
    - assets/device_frames/iphone-16-pro/
    - assets/device_frames/iphone-16-pro-max/
    - assets/device_frames/iphone-17/
    - assets/device_frames/iphone-17-pro/
    - assets/device_frames/iphone-17-pro-max/
    - assets/device_frames/iphone-air/
    - assets/device_frames/iphone-14/
    - assets/device_frames/iphone-14-plus/
    - assets/device_frames/iphone-14-pro/
    - assets/device_frames/iphone-14-pro-max/
    - assets/device_frames/ipad-pro-m5-11/
    - assets/device_frames/ipad-pro-m5-13/
    - assets/device_frames/ipad-air-11-m4/
    - assets/device_frames/ipad-air-13-m4/
    - assets/device_frames/ipad-mini-a17-pro/
    - assets/device_frames/ipad-a16/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
