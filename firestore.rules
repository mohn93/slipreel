rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the authenticated API or operator IAM credentials.
    match /{document=**} { allow read, write: if false; }
  }
}
